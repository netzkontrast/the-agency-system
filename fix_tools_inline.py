import sys
import os
import re

# We need to inline the missing tools.mastering and tools.state modules.
# Instead of doing that, which expands scope significantly or is hacky, Option A is requested, but
# "Wait, he said DO NOT GUESS"
