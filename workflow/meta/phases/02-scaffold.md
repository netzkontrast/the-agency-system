# Phase 02: Scaffold

Renders the three Jinja templates substituting `{{ new_row }}` everywhere, writes the three manifest files, writes empty handler stubs, returns `status="completed"` with the created paths.
