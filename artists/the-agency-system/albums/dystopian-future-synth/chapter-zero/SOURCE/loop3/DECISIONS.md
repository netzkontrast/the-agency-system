# Round 3 — Locked Decisions & Open Routing

All Round-3 agents (the three isolated Design agents, the Adversarial agent, the
Review / Listener / Spec-Panel agents, and the Opus arbiter) read this file plus
`SOURCE/loop/NAVIGATION.md` before acting.

## Baseline going into Round 3

- The working design is `DESIGN.md` (**v2**).
- The agreed, non-conflicting fixes are the **11-item work-order** in
  `SOURCE/loop2/arbiter.md` Section A — treat these as already-decided and fold
  them in (paradox ladder 0.84→0.99 not 0.67; restore name-quarantine to §7;
  add `0.41` spell-out; narrator-clip wording; phonetic-form ordering;
  Persona-per-section on T6/T11/T12; T9 flat-116 generation flag; bucket README
  prerequisite; + the no-objection accessibility enhancements A9–A11).

## LOCKED user decisions (do NOT re-litigate or re-clash)

These two were escalated to the user and decided. Every Round-3 agent treats
them as fixed constraints, on par with the hard rules.

1. **Track 10 (Resonance Cascade) = HYBRID "round → stack."** The suppressed
   echo-voices surface **one at a time** over a continuous drowning lead, each
   swamped by the next, and then **pile into a simultaneous wall at the
   climax.** Honors the source's *"alle Echos auf einmal"* as the peak moment
   while keeping the cascade legible early. (Resolves conflict B1.)
2. **Track 9 (Inward) = TIGHT / AIRLESS / MID-VOLUME.** The self-attack is
   staged as compressed, claustrophobic, contained — *not* loud/violent. The
   loudness explosion is saved for Track 10; Track 11 then implodes into a
   pulse-less drone. Back-half dynamic contour = **build → peak → void**
   (T9 airless → T10 peak → T11 implode). (Resolves conflict B2.)

## Open tensions routed to Design (decide with the arbiter's framing)

From `SOURCE/loop2/arbiter.md` Section B, tagged routine:

- **B3 — Mirror-echo role in T10.** With T10 now a round→stack, specify the
  mirror-echo's panning/timing so the proto-host/mirror pair stays legible
  during the sequential surfacing and the climactic stack.
- **B4 — T9 mastering/visual tier.** `section-meta` fixes the *state label* at
  **Tier 2 / ALERT** (this is ground truth, not open); what is open is whether
  T9's mastering bias and §6 palette follow the S2 state or its T3-energy
  character. Make an explicit call.
- **B5 — Number recitation in T9 lyrics.** The paradox-meter logs (0.84, 0.99)
  are source-authentic, but decimals may obscure the self-attack on a cold
  listen. Decide whether they are sung, spoken-low, or rendered as texture.

## Hard rules (the walls every option respects)

- **Source truth** for fidelity facts: `SOURCE/section-meta.md` → `SOURCE/kapitel-0.md`.
- **name_exposure**: function/role only in any output field (lyrics, Suno
  metatags, Style Boxes, promo, art prompts). Personal names live ONLY in
  `DESIGN.md §7` and the SOURCE files.
