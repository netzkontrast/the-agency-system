[project]
name = "session-log-mcp"
version = "0.1.0"
description = "Session log MCP server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.1.0",
    "pydantic>=2",
]
