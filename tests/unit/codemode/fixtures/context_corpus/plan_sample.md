---
spec_id: 111
slug: context-mode-manifest
domain: cross
---
# Spec 111 — Context Mode Manifest

## Why

This spec ships the manifest half of the pattern for context mode. It defines the `{id, title, summary, tags, path, mime, size_bytes, last_modified, sha256}` catalogue and the crawler that builds it.

## Done When

- The schema is done
- Tests are done
