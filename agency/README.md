# agency

An **installable Claude Code plugin** and a running engine for the v4 core (see
`../vision-v3/CORE.md`) on the **real substrate** — `graphqlite` (SQLite +
Cypher) + `fastmcp` (incl. the experimental code-mode transform). One bi-temporal
provenance graph, four concepts (Intent · Capability · Lifecycle · Memory), and
**code-mode as the one contract**, exposed isomorphically over MCP · Skills · a
bash CLI.

> This graduated from the `agency-seed` proof-of-concept. It is early, but it is
> the real thing: capabilities self-register by reflection, and the engine
> **authors and validates its own plugin install**.

## Install (Claude Code)

The plugin manifest, the `help` macroskill, and the `/agency:help` command are
**generated by the engine itself** (dogfooding the plugin-development
capability) and committed at the plugin root:

```
.claude-plugin/plugin.json     # the install descriptor (name: agency)
skills/help/SKILL.md           # the help macroskill (maps capabilities -> verbs)
commands/help.md               # /agency:help
```

Add this directory as a plugin (e.g. via a marketplace entry whose `source`
points here), or regenerate the install after adding capabilities:

```bash
python -m agency.install            # rewrites the three files from the live registry
```

## What it proves (`tests/test_agency.py`, 17/17 green)

1. **The moat — cross-concern provenance is one graph traversal.**
   `Memory.provenance(intent)` returns, in one Cypher walk, every action that
   `SERVES` the intent, the agent that `PERFORMED_BY` it, the artefact it
   `PRODUCES`d, and the gate it `PASSED`.
2. **One graph + the verb frame carry two genuinely different capabilities** — a
   synchronous craft/compute (`plugin`) and a remote async `agent` (`jules`).
3. **Bi-temporal memory** — the *what* changes while the *why* holds; the prior
   version is reconstructable `as_of` an earlier tick.
4. **`COMPLETED != done`** — the Jules silent-fail lesson as a first-class
   `verify` step (done only if state completed AND a branch is on origin).
5. **Code-mode IS the contract** — the engine surface is exactly
   `search` / `get_schema` / `execute`; tools are discovered via `search` and
   called from inside `execute`.
6. **Code-mode tool-chaining = an executable graph** — an `execute` block chains
   `transform → agent` in plain Python; calls run in-sandbox and only a small
   delta crosses into context (token efficiency), while every call records an
   Invocation, mirroring the dataflow graph into durable provenance.
7. **Gates / human-in-the-loop via `elicit`** — a gate asks a one-line question
   mid-flow and records the outcome as a `Gate`.
8. **Bash ≡ MCP isomorphism** — the same code-mode contract via MCP *and* via a
   bash-only subprocess, over the same graph, yields identical results (`AGENTS.md`).
9. **Schemas & templates (typed/generative layer)** — a Template generates an
   Artefact (`DERIVED_FROM`) that a Schema validates (`VALIDATES_AGAINST`).
10. **A strictly-enforced ontology** (`ontology.py`) — per-node required-field
    schemas + an enumerated edge set + closed enums; `record`/`link`/`update`
    reject drift.
11. **A micro-step skill walker** (`skill.py`) — walks a skill one phase at a
    time (progressive disclosure), executes phases bound to REAL capability
    verbs, and blocks at a hard gate until confirmed; every phase is provenance.
12. **Capabilities self-register by reflection** — the engine `discover()`s every
    `Capability` in `capabilities/` and AUTO-WIRES one MCP tool per verb from the
    verb's signature (`inspect.signature`). Adding a capability is adding a file.
13. **The plugin-development capability** — a complete port of the superpowers
    `writing-skills` (skill creation) + plugin/marketplace authoring: `scaffold`
    a manifest, `author_skill` a SKILL.md, `author_command`, `marketplace_entry`,
    and `lint_skill` (the CSO rules as enforceable compute).
14. **The skill-creation skill, with its Iron Law enforced structurally** — the
    `SKILL_CREATION_SKILL` cannot reach GREEN (authoring) until RED (the failing
    baseline) has produced its outputs; ordering *is* the rule.
15. **The plugin-dev chain prestructures each step's document** — manifest →
    skill → command → marketplace entry → hard confirm gate, each step a strict,
    provenance-recorded artefact.
16. **The macroskill/micro-skill map (`help`)** — the harness-in-harness
    micro-skills (verbs) grouped under their macroskills (capabilities).
17. **The install is self-hosted** — the committed manifest, help skill, and
    command are exactly what the capability regenerates from the live registry,
    and the help skill passes its own CSO linter.

## Run

```bash
python -m venv .venv && . .venv/bin/activate
pip install -r requirements.txt
pytest -q
```

## Layout

| Path | Concept |
|---|---|
| `agency/memory.py` | **Memory** — bi-temporal graph; `record·link·supersede` / `recall·find·validate`; `project` (budgeted); `provenance` (the moat) |
| `agency/intent.py` | **Intent** — `capture·confirm·amend` (why/what merged) |
| `agency/lifecycle.py` | **Lifecycle** — `open·move·complete·status`; A2A-aligned states; gates |
| `agency/capability.py` + `capabilities/` | **Capability** — open verbs, role-tagged `act·transform·effect`; **discovered by reflection** |
| `agency/capabilities/plugin.py` | the **plugin-development** capability (skill creator + plugin authoring) |
| `agency/capabilities/jules.py` | the **jules** agent capability (real remote async sessions) |
| `agency/templates.py` | **Templates** — prestructured documents (manifest · SKILL.md · command · step-doc) |
| `agency/ontology.py` | the strict ontology + skill schemas |
| `agency/skill.py` | the micro-step skill walker (progressive disclosure + hard gate) |
| `agency/engine.py` | **Engine** — FastMCP; code-mode contract; reflection-based auto-wiring |
| `agency/cli.py` + `AGENTS.md` | **Bash layer** — the same code-mode contract for bash-only agents (Jules) |
| `agency/install.py` | the self-hosted Claude Code plugin install (generated by the engine) |
